package com.inherex;
/*
 * 상속(inheritance)
 * 		- 기존의 클래스를 재사용하여 새로운 클래스를 작성하는 것을 의미함
 * 		
 * 		조상클래스 → 부모클래스(parent),상위클래스(super),기반클래스(base)
 * 		자손클래스 → 자식클래스(child),하위클래스(sub),파생클래스(derived)
 * 		
 * 
 * 
 * 
 * */
public class Parent {

	protected	int age;

}
