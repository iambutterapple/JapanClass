package com.classex;

public class Circle {
	// 필드 선언
	int radius; // 인스턴스 변수: 인스턴스(객체)를 생성해야만 접근이 가능한 변수
	String name;
					// 클래스 변수: 인스턴스(객체)를 생성하지 않고 클래스명 만으로도 접근이 가능한 변수
	
	public Circle() { // 기본 생성자
		
	}
	
	// 원의 크기를 계산하는 메소드
	// 원의 넓이: 반지름 * 반지름 * 3.14
	public double getArea() {
		return radius * radius * 3.14;
	}
	
	public static void main(String[] args) {
		
	}
}
