package com.classex;

/*
 *	생성자
 *		-메모리 내에 객체가 생성될 때 호출되어 객체의 구조를 인식하게 하고
 *		생성되는 멤버 변수들을 초기화 작업을 하는데 그 목적을 둔다.	
 *		-생성자명은 클래스명과 같아야 하고, 반환형을 정의하지 않는다.
 *		-프로그래머가 어떠한 생성자도 정의하지 않았을 경우 컴파일러가 기본 생성자를
 *		자동으로 만들어 줌
 *		기본 생성자: 매개변수 없는 생성자를 의미함
 *
 *	생성자의 접근제한의 의미
 *		-생성자의 접근을 제한을 둘 경우 해당 객체를 생성할 수 있는 접근 권한을 가짐
 *		-클래스의 접근제한이 public 으로 정의되어도 private 로 정의하면
 *		클래스 내부에서만 접근이 가능하다.
 *		-만약 protected 로 정의되는 클래스는 상속관계의 객체들만 생성할 수 있음
 *		
 *	생성자의 구성
 *		[접근 제한자]	[생성자명] (자료형 인자1, 자료형 인자2) {
 *			수행문;
 *			수행문;
 *		}
 *
 */

class MyClass {
	
}

public class MyClassEx {

	public static void main(String[] args) {
		
		
	}

}
