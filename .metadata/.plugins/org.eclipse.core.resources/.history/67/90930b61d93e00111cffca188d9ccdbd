package com.classex.ex01;

/*
 *  문]
 *  		다음 멤버필드를 가지고 있는 직사각형을 표현 Rectangle 클래스 작성
 *			
 *		1.	int 타입의 x,y,width,height 값을 매개변수로 받아 필드를
 *			초기화 하는 생성자 
 *		2. int square(); 사각형의 넓이를 리턴
 *		3. void show(); 사각형의 좌표와 넓이를 화면에 출력
 *		4. boolean contains(Rectangle r); 매개변수로 받은 참조변수 r이
 *			현재의 사각형안에 있으면 true를 리턴, 사각형을 벗어났으면 false 
 * 
 * 		main() 메소드를 아래와 같다.
 * 
 * 				public stati void main(String args[]){
 * 				
 * 				Rectangle r = new Rectangle(2,2,8,7);		
 * 				Rectangle s = new Rectangle(5,5,6,6);		
 * 				Rectangle st = new Rectangle(1,2,10,10);		
 * 
 * 				r.show();
 * 				System.out.print("s의 면적은 "+s.square());
 * 				
 * 				if(t.contains(r))
 * 					System.out.println("t는 r을 포함한다.");
 * 				if(t.contains(s))
 * 					System.out.println("t는 ss을 포함한다.");
 * 
 * 
 * 
 * 				출력결과
 * 				(2,2)에서 크기가 8 * 7인 사각형
 * 				s의 면적은 36
 * 				t는 r을 포함한다.
 * 
 * 
 * 				}
 * 
 * 
 * */

public class Rectangle {
	
	//1.	int 타입의 x,y,width,height 값을 매개변수로 받아 필드를 초기화 하는 생성자 
	private int x,y,width,height;


	public Rectangle(int a,int b,int w,int h) {
		this.x = a;//x축
		this.y = b;//y축
		this.width = w;//width
		this.height = h;//height
		//tests
		System.out.println(x+""+y+""+width+""height);
	}
//	2. int square(); 사각형의 넓이를 리턴
	public int square() {
		return 0;
	}
	public static void main(String[] args) {

		Rectangle r = new Rectangle(2,2,8,7);		
		Rectangle s = new Rectangle(5,5,6,6);		
		Rectangle st = new Rectangle(1,2,10,10);		
		  
//		r.show();
//		System.out.print("s의 면적은 "+s.square());
		  				
//		if(t.contains(r))
//				System.out.println("t는 r을 포함한다.");
//		 if(t.contains(s))
//			 	System.out.println("t는 ss을 포함한다.");
		 
		  
		  
		 // 				출력결과
		 //					(2,2)에서 크기가 8 * 7인 사각형
		 // 				s의 면적은 36
		 // 				t는 r을 포함한다.
	}

}
