package com.classex;

/*
 * 	접근 제한자(접근 제어 변경자)
 * 		-멤버(필드, 메소드)들은 객체 자신들만의 속성이자 특징이므로 대외적으로 공개되는 것이
 * 			결코 좋은 것이 아니다. 그런 이유로 프로그래머가 객체의 멤버들에게 접근 제한을 
 * 			걸 수 있는
 */

public class SalaryEx {

	public static void main(String[] args) {
		
		
	}

}
